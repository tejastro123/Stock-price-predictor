# ============================================================
# Stock Price Prediction System — Environment Variables
# ============================================================
# Copy this file to .env and customise as needed:
#   cp .env.example .env
# ============================================================

# ── FastAPI ML API ──────────────────────────────────────────
ML_API_HOST=0.0.0.0
ML_API_PORT=8000
# Comma-separated list of allowed CORS origins (use * for all)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ── Express Server ──────────────────────────────────────────
PORT=5000
MONGODB_URI=mongodb://localhost:27017/stock_predictor
# URL where FastAPI is reachable from Express
ML_API_URL=http://localhost:8000

# ── React Client (Vite) ────────────────────────────────────
# Leave blank to use the Vite /api proxy (recommended for dev)
# Set to a full URL for production without proxy, e.g. http://api.example.com
VITE_API_BASE_URL=

# ── Docker Compose Ports (optional overrides) ──────────────
# MONGO_PORT=27017
# CLIENT_PORT=3000
