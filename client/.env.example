# React Client Environment Variables
# ===================================
# Copy to .env:  cp .env.example .env

# API base URL — leave blank to use the Vite dev proxy (/api)
# Set to a full URL for production (e.g. https://api.example.com/api)
VITE_API_BASE_URL=
